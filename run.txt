every_country = {    
        export_to_variable = { #each country income
            which = numX
            value = trigger_value:monthly_income
        }
		set_variable = {
			num = 300
		}
		if = {
			check_variable = {
				num = 300
			}
			add_prestige = 50
		}
		
    }
