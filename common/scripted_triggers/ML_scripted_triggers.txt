sripted_trigger_loc_dev_cost_less_than = {
	#cheks if value is lower than the devcost
	variable_arithmetic_trigger = {
		export_to_variable = {
			variable_name = varibale	
			value = modifier:local_development_cost
		} #import mod
		NOT = {
			check_variable = {
				which = varibale
				value =  $value$
			}
		}
	}
}
	



is_SPEC_TAG = {
	OR = {
		tag = REB
		tag = NAT
		tag = PIR
}	} 

trigger_of_monarch_power = {
	REB = {
		if = { limit = { NOT = { has_institution = feudalism } } 
			variable_arithmetic_trigger = {
				export_to_variable = {
					variable_name = power
					value = trigger_value:$power_type$
					who = PREV
				}
				export_to_variable = {
					variable_name = powerCost
					value = modifier:all_power_cost
					who = PREV
				}
				change_variable = {
					which = powerCost
					which = feudalism_no
				}
				multiply_variable = {
					which = powerCost		#buffer so AI clicks earlier
					which = dev_click_max	#scaled tech cost
				}
				check_variable = {
					which = power
					which = powerCost
				}
			}  
			$power_type$ = $min$
		}
		else_if = { limit = { NOT = { has_institution = renaissance } } 
			variable_arithmetic_trigger = {
				export_to_variable = {
					variable_name = power
					value = trigger_value:$power_type$
					who = PREV
				}
				export_to_variable = {
					variable_name = powerCost
					value = modifier:all_power_cost
					who = PREV
				}
				change_variable = {
					which = powerCost
					which = renaissance_no
				}
				multiply_variable = {
					which = powerCost		#buffer so AI clicks earlier
					which = dev_click_max	#scaled tech cost
				}
				check_variable = {
					which = power
					which = powerCost
				}
			}  
			$power_type$ = $min$
		}
		else_if = { limit = { NOT = { has_institution = new_world_i } } 
			variable_arithmetic_trigger = {
				export_to_variable = {
					variable_name = power
					value = trigger_value:$power_type$
					who = PREV
				}
				export_to_variable = {
					variable_name = powerCost
					value = modifier:all_power_cost
					who = PREV
				}
				change_variable = {
					which = powerCost
					which = new_world_i_no
				}
				multiply_variable = {
					which = powerCost		#buffer so AI clicks earlier
					which = dev_click_max	#scaled tech cost
				}
				check_variable = {
					which = power
					which = powerCost
				}
			}  
			$power_type$ = $min$
		}
		else_if = { limit = { NOT = { has_institution = printing_press } } 
			variable_arithmetic_trigger = {
				export_to_variable = {
					variable_name = power
					value = trigger_value:$power_type$
					who = PREV
				}
				export_to_variable = {
					variable_name = powerCost
					value = modifier:all_power_cost
					who = PREV
				}
				change_variable = {
					which = powerCost
					which = printing_press_no
				}
				multiply_variable = {
					which = powerCost		#buffer so AI clicks earlier
					which = dev_click_max	#scaled tech cost
				}
				check_variable = {
					which = power
					which = powerCost
				}
			}  
			$power_type$ = $min$
		}
		else_if = { limit = { NOT = { has_institution = global_trade } } 
			variable_arithmetic_trigger = {
				export_to_variable = {
					variable_name = power
					value = trigger_value:$power_type$
					who = PREV
				}
				export_to_variable = {
					variable_name = powerCost
					value = modifier:all_power_cost
					who = PREV
				}
				change_variable = {
					which = powerCost
					which = global_trade_no
				}
				multiply_variable = {
					which = powerCost		#buffer so AI clicks earlier
					which = dev_click_max	#scaled tech cost
				}
				check_variable = {
					which = power
					which = powerCost
				}
			}  
			$power_type$ = $min$
		}
		else_if = { limit = { NOT = { has_institution = manufactories } } 
			variable_arithmetic_trigger = {
				export_to_variable = {
					variable_name = power
					value = trigger_value:$power_type$
					who = PREV
				}
				export_to_variable = {
					variable_name = powerCost
					value = modifier:all_power_cost
					who = PREV
				}
				change_variable = {
					which = powerCost
					which = manufactories_no
				}
				multiply_variable = {
					which = powerCost		#buffer so AI clicks earlier
					which = dev_click_max	#scaled tech cost
				}
				check_variable = {
					which = power
					which = powerCost
				}
			}  
			$power_type$ = $min$
		}
		else_if = { limit = { NOT = { has_institution = enlightenment } } 
			variable_arithmetic_trigger = {
				export_to_variable = {
					variable_name = power
					value = trigger_value:$power_type$
					who = PREV
				}
				export_to_variable = {
					variable_name = powerCost
					value = modifier:all_power_cost
					who = PREV
				}
				change_variable = {
					which = powerCost
					which = enlightenment_no
				}
				multiply_variable = {
					which = powerCost		#buffer so AI clicks earlier
					which = dev_click_max	#scaled tech cost
				}
				check_variable = {
					which = power
					which = powerCost
				}
			}  
			$power_type$ = $min$
		}
		else 	= {
			variable_arithmetic_trigger = {
				export_to_variable = {
					variable_name = power
					value = trigger_value:$power_type$
					who = PREV
				}
				export_to_variable = {
					variable_name = powerCost
					value = modifier:all_power_cost
					who = PREV
				}
				change_variable = {
					which = powerCost
					value = 1
				}
				multiply_variable = {
					which = powerCost		#buffer so AI clicks earlier
					which = dev_click_max	#scaled tech cost
				}
				check_variable = {
					which = power
					which = powerCost
				}
			}  
			$power_type$ = $min$
		}
	}
}
