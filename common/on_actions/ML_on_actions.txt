on_startup = {
	events = {
		muslim_school_events.20 #Pick School
		Counter_institutions.1  #start of lowerst counter
		increase_dev_event.1 # start of lowerst dev provence modded in by mellester
	}
	if = {
		limit = {
			has_dlc = "Cradle of Civilization"
			has_country_modifier = tur_janissary
		}
		remove_country_modifier = tur_janissary
	}
	initialize_schools_effect = yes #This is only used here but is used for readability of on_action file.
	
		if = {
		limit = {
			country_has_both_paradox_and_me_missions = yes
		}
		country_event = { id = missions_expanded_events.1 }
	}
		country_event = { id = reform_me_events.137 } #set_global_flag = governments_expanded_mod_active

	SWE = {
		set_variable = { swe_num_of_wars_against_non_catholics = 0 }
	}
	if = {
		limit = {
			exists = AVR
		}
		AVR = {
			country_event = { id = ME_Avaria_Events.1 }
		}
	}
	if = {
		limit = {
			tag = TKD
		}
		change_variable = { which = num_of_won_battles value = 0 }
	}

	
	
	if = {
			limit = {
			ai = no
			}
			
		country_event = { id = idea_settings.1 }	
	
	}
	
	if = {
			limit = {
			ai = yes
			}
			
		country_event = { id = mp_mod_events.1 }	
	
	}

}

# province
on_adm_development = {
	if = {
		limit = { has_province_flag = CheapDevTargetFlag }
		owner = { clr_country_flag = JustDevedFLAG }
		HiddenUpdateDevFlagEffect = {
		   target=target
	   }
	}
}

# province
on_dip_development = {	
 if= {
	 limit = { has_province_flag = CheapDevTargetFlag}
	 owner = { clr_country_flag = JustDevedFLAG }
	 HiddenUpdateDevFlagEffect = {
		target=target
	}
 }
}

# province
on_mil_development = {
	if = {
		limit = { has_province_flag = CheapDevTargetFlag }
		owner = { clr_country_flag = JustDevedFLAG }
		HiddenUpdateDevFlagEffect = {
		   target=target
	   }
	}
}

