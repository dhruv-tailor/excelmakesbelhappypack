# Audax Validator "!" Ignore_1006
# ideas of specific idea groups are added
# Ideas in this file: early ideas from Extended Timeline

# Template:
#add_ideas_of_idea_group_<idea group name> = {
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of first idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of second idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of third idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of fourth idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of fifth idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of sixth idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of seventh idea of idea group>
#	}
#}

add_ideas_of_idea_group_early_adm_ideas = {
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = slavery
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = decentralization
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = advisors
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = resistance_to_foreigners
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = efficient_administration
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = administrative_innovation
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = noble_soldiers
	}
}

add_ideas_of_idea_group_early_religious_ideas = {
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = early_tolerance
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = converting
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = acceptance_of_differences
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = religious_customs
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = religious_education
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = divine_heritage
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = organized_religion
	}
}

add_ideas_of_idea_group_early_dip_ideas = {
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = diplomats
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = additional_relations
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = vassal_tribute
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = diplomatic_reputation
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = foreign_customs
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = claims
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = diplomatic_conquest
	}
}

add_ideas_of_idea_group_early_trade_ideas = {
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = trading
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = galley_slaves
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = merchant_admirals
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = naval_innovation
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = merchant_travels
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = protected_trade
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = shipwrights
	}
}

add_ideas_of_idea_group_early_mil_ideas = {
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = formation
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = professional_soldiers
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = military_education
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = horse_breeding
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = early_mercenaries
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = military_innovation
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = cheaper_mercenaries
	}
}

add_ideas_of_idea_group_early_defensive_ideas = {
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = border_guard
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = no_aid_for_enemy
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = fight_for_our_land
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = history_of_independence
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = recruitment
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = slave_army
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = fortifications
	}
}