# Audax Validator "!" Ignore_1006
# ideas of specific idea groups are added
# Ideas in this file: late ideas from Extended Timeline

# Template:
#add_ideas_of_idea_group_<idea group name> = {
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of first idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of second idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of third idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of fourth idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of fifth idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of sixth idea of idea group>
#	}
#	if = {
#		limit = {
#			check_variable = { which = num_of_overlord_ideas value = 1 }
#		}
#		change_variable = { which = num_of_overlord_ideas value = -1 }
#		add_idea = <name of seventh idea of idea group>
#	}
#}

add_ideas_of_idea_group_order_ideas = {
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = state_controlled_economy
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = patriotism
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = state_police
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = workers_utopia
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = free_education
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = military_service
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = universal_healthcare
	}
}

add_ideas_of_idea_group_freedom_ideas = {
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = capitalist_economy
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = free_religion
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = universal_suffrage
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = freedom_of_speech
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = equality_of_races
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = volunteer_army
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = sharing_of_ideas
	}
}

add_ideas_of_idea_group_autocracy_ideas = {
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = glorious_leader
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = police_state
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = elite_generals
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = military_industry
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = military_thinkers
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = resettlement
	}
	if = {
		limit = {
			check_variable = { which = num_of_overlord_ideas value = 1 }
		}
		change_variable = { which = num_of_overlord_ideas value = -1 }
		add_idea = total_war
	}
}