namespace = ML_MillAcces
country_event = {
	id = ML_MillAcces.1
	title = "name"
	desc = "name"
	hidden = yes
	trigger = {
		ai = no
		OR = {
			had_country_flag = { 
				flag = modifiertimer
			 days = 31
			} 
		}
	}
	mean_time_to_happen = {
		days = 1
	}
	immediate = {
		log = "fired for [This.GetName]"
	}
	option = {
		clr_country_flag = modifiertimer
		set_country_flag = modifiertimer
		set_variable = { which = MillAcces_var value = 0 }
		every_known_country = {
			limit = {
				OR = { 
					AND = {
						ai = yes
						has_opinion_modifier = {
							who = ROOT
							modifier = opinion_military_access
					}	 }	
					ROOT = {
						has_opinion_modifier = {
							who = PREV
							modifier = opinion_military_access
						}					
				}	}
			 }
			ROOT = { change_variable = { which = MillAcces_var value = 1 } }
		}

		set_variable = { which = temp value = 0 } #clean up
		# common\scripted_effects\ML_BinaryMod.txt
		log = " var is [This.MillAcces_var.GetValue]"
		refresh_binary_modifier = { name=millacces variable=MillAcces_var scope=country }
	}
}