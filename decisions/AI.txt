country_decisions = {
	dev_click = {
		potential = {
			OR = {
				#ai = yes
				always = yes
			}
		}
		allow = {
			NOT = {
				has_country_flag = JustDevved
			}
			OR = {
				power_at_cap = {value=998 type=adm }
				power_at_cap = {value=998 type=mil }
				power_at_cap = {value=998 type=dip }
				always = yes
			}
		}
		provinces_to_highlight = {
			OR = {
			has_province_flag = CheapDevTargetFlag
			has_province_flag = productionTargetFlag
			has_province_flag = manpowerTargetFlag
			has_province_flag = taxTargetFlag
			}
		}
		effect = {
			country_event = {
				id = increase_dev_event.1
				days = 0
			}
			
		}
		ai_will_do = {
			factor = 1
		}
	}
}